# Запуск скрипта

## 1. Заполнить креды
Создайте файл `.env` и укажите в нём необходимые параметры:


## 2. Загрузить переменные окружения

set -o allexport
source path_to_env

Где `path_to_env` — путь к вашему `.env` файлу.

## 3. Запуск скрипта
python main.py
